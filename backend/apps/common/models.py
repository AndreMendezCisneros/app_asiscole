"""Modelos compartidos.

Por ahora vacio a proposito. Si mas adelante hacen falta piezas transversales
(por ejemplo una clase base abstracta con `creado_en` / `actualizado_en`), van
aqui. Cualquier tabla nueva del canal se crea en la BD central y con prefijo
`asis_` en `db_table`.
"""
